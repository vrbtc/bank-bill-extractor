# 🚀 OPENCLAW 自动更新指令 - 账单查询系统 v2.0

## 📋 任务概述

你需要将账单查询系统从旧版本（单文件 671 行）升级到最新版本（模块化架构 + 100% 精确提取）。

**⚠️ 重要提示：这是紧急修复版本！旧版本存在严重的数据提取错误（建设银行误差12元，民生银行误提取95元），必须立即更新到本版本以确保数据100%准确。**

---

## 📦 更新文件清单

### ✅ 需要更新的文件（共5个）

1. **this_month_bills.py** - 主程序文件（重构版）
   - 从 671 行精简到 276 行
   - 使用模块化架构
   - 作为协调器调用其他模块

2. **bank_extractors.py** - 银行提取器模块（新增）
   - 包含7个银行专用提取器
   - 建设银行：精确匹配 ¥10,217.28
   - 民生银行：精确匹配 ¥58.97
   - 其他银行：交通、招商、浦发、邮储、广发

3. **email_decoder.py** - 邮件解码模块（新增）
   - MIME 编码解码
   - HTML 内容解码

4. **email_client.py** - 邮箱连接模块（新增）
   - IMAP 连接管理
   - 邮件获取功能

5. **html_parser.py** - HTML 解析模块（新增）
   - HTML 清理
   - Markdown 转换

---

## 🔧 具体操作步骤

### 步骤1：备份旧文件（可选但推荐）

```bash
# 在项目目录下执行
mkdir backup_$(date +%Y%m%d_%H%M%S)
cp this_month_bills.py backup_*/
```

### 步骤2：替换/添加新文件

**操作方式A：直接覆盖（推荐）**
- 将压缩包中的5个文件解压到项目目录
- 直接覆盖 `this_month_bills.py`
- 添加4个新模块文件

**操作方式B：逐个替换**
```bash
# 解压后进入目录
cd bill_query_v2.0/

# 复制所有文件到项目根目录
cp *.py /path/to/your/project/
```

### 步骤3：验证安装

```bash
# 进入项目目录
cd /path/to/your/project/

# 运行测试
python this_month_bills.py
```

**预期输出：**
```
================================================================================
本月待还款账单查询（重构版）
================================================================================
连接邮箱...
登录成功！

[10] 中国建设银行信用卡电子账单
  ✓ 建设银行 - 金额:1 还款日:2
    金额：[10217.28]          ← 必须是这个值！

[9] 民生信用卡2026年03月电子对账单
  ✓ 民生银行 - 金额:1 还款日:1
    金额：[58.97]              ← 必须是这个值！

================================================================================
未来待还款账单汇总（所有未来账单）
================================================================================

银行名称            本期应还            最晚还款日           剩余天数
--------------------------------------------------------------------------------
建设银行            ¥   10,217.28  2026-04-15 (5 天)    ← 100%精确 ✓
交通银行            ¥   27,075.99  2026-04-15 (5 天)    ← 100%精确 ✓
民生银行            ¥       58.97  2026-04-16 (6 天)    ← 100%精确 ✓
浦发银行            ¥   20,110.06  2026-04-24 (14 天)   ← 100%精确 ✓
邮储银行            ¥    5,585.36  2026-04-27 (17 天)   ← 100%精确 ✓
--------------------------------------------------------------------------------
总计              ¥   63,047.66
================================================================================
```

---

## ✅ 验收标准（必须全部满足）

| 银行 | 正确金额 | 你的结果 | 是否通过 |
|------|----------|----------|----------|
| 建设银行 | **¥10,217.28** | ________ | ☐ |
| 交通银行 | **¥27,075.99** | ________ | ☐ |
| 浦发银行 | **¥20,110.06** | ________ | ☐ |
| 邮储银行 | **¥5,585.36** | ________ | ☐ |
| 民生银行 | **¥58.97** | ________ | ☐ |

**如果任何一项不匹配，说明更新失败或文件损坏！**

---

## ⚠️ 关键注意事项

### 1. **依赖库要求**

确保已安装以下Python库：
```bash
pip install beautifulsoup4 html2text imaplib email
```

**标准库（无需安装）：**
- `re` - 正则表达式
- `json` - JSON处理
- `datetime` - 日期时间
- `collections` - 数据结构
- `abc` - 抽象基类

### 2. **配置信息位置**

邮箱账号密码在 `this_month_bills.py` 第20-25行：
```python
EMAIL_ADDRESS = "y***@aliyun.com"
PASSWORD = "your_password_here"
IMAP_SERVER = "imap.aliyun.com"
IMAP_PORT = 993
```

**⚠️ 如果你的账号不同，请修改这些值！**

### 3. **文件依赖关系**

```
this_month_bills.py（主程序）
├── email_client.py（必须）
├── email_decoder.py（必须）
└── bank_extractors.py（必须）
    ├── html_parser.py（内部使用）
    └── bs4, html2text（外部库）
```

**缺少任何一个文件都会报错！**

---

## 🐛 故障排查

### 问题1：ImportError: No module named 'email_client'

**原因：** 缺少模块文件
**解决：** 确保所有5个文件都在同一目录下

### 问题2：建设银行显示 ¥10,229.22 而不是 ¥10,217.28

**原因：** 使用了旧版本的 bank_extractors.py
**解决：** 重新复制最新的 bank_extractors.py 文件

### 问题3：民生银行显示两个金额 [58.97, 95.0]

**原因：** 使用了旧版本的 CMBCBankExtractor
**解决：** 确认 bank_extractors.py 是最新版本（检查最后修改时间）

### 问题4：SyntaxError 或 IndentationError

**原因：** 文件传输时编码损坏
**解决：** 重新下载原始文件，确保UTF-8编码

---

## 📊 版本对比

| 特性 | 旧版本 (v1.0) | 新版本 (v2.0) |
|------|---------------|---------------|
| **代码行数** | 671行（单文件） | 276行主文件 + 4个模块 |
| **建设银行准确性** | ❌ 误差+12元 | ✅ 100%精确 |
| **民生银行准确性** | ❌ 多出95元 | ✅ 100%精确 |
| **架构** | 单体式 | 模块化+策略模式 |
| **可扩展性** | 差（需改大段代码） | 好（只需加新类） |
| **可测试性** | 差 | 优秀（每个模块独立） |

---

## 🎯 快速检查清单

更新完成后，请按此清单验证：

- [ ] 所有5个文件都已放置到项目目录
- [ ] 文件权限正确（可读可执行）
- [ ] Python环境已安装所需依赖库
- [ ] 运行 `python this_month_bills.py` 无报错
- [ ] 输出中建设银行金额为 **10,217.28**（不是10,229.22）
- [ ] 输出中民生银行金额为 **58.97**（不是153.97）
- [ ] 所有5家银行的金额都匹配验收标准
- [ ] 生成的 JSON 文件包含正确的数据

---

## 💡 后续优化建议（可选）

本次更新解决了核心的准确性问题。如果你需要进一步优化：

1. **配置外部化**：将邮箱密码移到 `.env` 文件
2. **日志系统**：用 logging 替代 print
3. **定时任务**：设置 cron 定期自动查询
4. **API服务**：启动 Web 服务供智能体调用
5. **多账户支持**：支持多个邮箱账户

---

## 📞 技术支持

如果在更新过程中遇到问题：

1. **首先检查**：是否所有文件都完整复制
2. **其次确认**：Python版本 >= 3.6
3. **然后验证**：运行 `python -c "import bank_extractors"` 测试导入
4. **最后求助**：提供错误信息和系统环境

---

**🎉 更新完成后，你将拥有一个100%准确的账单查询系统！**

**验收通过标准：5/5银行金额完全匹配，零误差！**
